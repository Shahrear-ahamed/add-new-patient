import React from 'react';
import Box from  "@mui/material/Box"
import Typography from "@mui/material/Typography";

const AutoComplete = () => {
    return (
        <Box>
            <Typography variant="h4" align="center" my={3}>
                This is a AutoComplete section
            </Typography>
        </Box>
    );
};

export default AutoComplete;
