import React from 'react';
import "../global.css"

const AddNewPatient = () => {
    return (
        <div className="header-title-div">
            <h2 className="header-top-title">Patient | Add New</h2>
        </div>
    );
};

export default AddNewPatient;
